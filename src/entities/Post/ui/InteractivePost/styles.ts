import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    aspectRatio: 1,
    objectFit: 'cover',
    position: 'relative',
  },
  image: {
    width: '100%',
    height: '100%',
  },
});
