import {View, Text} from 'react-native';
import React from 'react';
import {LikeButtonProps} from '../model/types/types';

export default function LikeButton({id}: LikeButtonProps) {
  return (
    <View>
      <Text>LikeButton</Text>
    </View>
  );
}
