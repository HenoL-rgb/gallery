import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    paddingVertical: 10,
  },
  contentContainer: {
    paddingVertical: 5,
    paddingHorizontal: 15,
    columnGap: 15,
  },
});
