import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {},
  contentContainer: {
    gap: 2,
    paddingBottom: 20,
  },
  columnWrapper: {
    gap: 2,
  },
  indicator: {
    marginTop: 10,
  }
});
